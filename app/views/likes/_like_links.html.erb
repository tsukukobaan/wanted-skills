<% if current_user.likes.find_by(skill_id: skill.id) %>
  <%= link_to 'いいね！を取り消す', unlike_path(skill.id), method: :delete, remote: true %>
<% else %>
  <%= link_to 'いいね！', like_path(skill.id), method: :post, remote: true %>
<% end %>